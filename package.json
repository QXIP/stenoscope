{
  "name": "stenoscope",
  "version": "1.0.0",
  "description": "Stenographer SSTableKey Reader w/ GO Binding",
  "main": "index.js",
  "scripts": {
    "build": "node-gyp configure build",
    "install": "node-gyp-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
	"stenographer",
	"pcap",
	"go",
	"sstable"
  ],
  "license": "MIT",
  "devDependencies": {
    "node-gyp": "^3.8.0"
  },
  "dependencies": {
    "node-gyp-build": "^4.2.3"
  }
}
